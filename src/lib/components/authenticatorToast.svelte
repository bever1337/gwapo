<script lang="ts">
</script>

<aside class="aside">
  <p class="message">Sign in to view your wallet.</p>
  <form />
</aside>

<style>
  .aside {
    background-color: rgb(var(--primary--200));
    border: 0px solid transparent;
    box-shadow: inset 0 0.5rem 0.5rem -0.25rem rgb(var(--black) / 0.3), var(--elevation--5);
    box-sizing: border-box;
  }

  .message {
    box-sizing: border-box;
    margin: 0 auto;
    max-width: calc(104rem + calc(2 * var(--margin)));
    padding: var(--gutter) var(--margin);
    text-align: left;
    width: 100%;
  }
</style>
